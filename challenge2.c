/*#include <stdio.h>
#include <stdlib.h>

int main()
{
    int arr[10];
    int max;
    int min=arr[0];
    for (int i=0; i<10; i++)
    {
        scanf("%d,", &arr[i]);

        if (arr[i]>=max)
            max=arr[i];
        if(arr[i]<min)
            min=arr[i];

    }
    for (int i=0; i<10; i++)
    {
        printf("%d-",arr[i]);
    }
    printf("\n min= %d\n",min);
    printf("max =%d",max);

    return 0;
}
*/

